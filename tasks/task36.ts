// Define the makeShirt function
function MakeShirt(size: string, message: string): void {
    console.log(`A ${size}-sized shirt will be made with the message '${message}' printed on it.`);
  }
  
  // Call the makeShirt function
  MakeShirt("medium", "Itachi Uchiha");